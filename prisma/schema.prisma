// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 📌 Modèle des Comptes Utilisateurs (Authentification OAuth)

model Account {
  id                String  @id @default(cuid())
  userId            String  @map("user_id")
  type              String
  provider          String
  providerAccountId String  @map("provider_account_id")
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

// 📌 Modèle des Sessions Utilisateurs

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique @map("session_token")
  userId       String   @map("user_id")
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

// 📌 Modèle des Utilisateurs

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime? @map("email_verified")
  password      String?
  image         String?

  accounts Account[]
  sessions Session[]
  invoices Invoice[] @relation("UserInvoices")

  @@map("users")
}

// 📌 Modèle pour les Tokens de Vérification (Email, Authentification à Deux Facteurs...)

model VerificationToken {
  identifier String
  token      String
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

//🔹 Enum pour les statuts des factures
enum InvoiceStatus {
  PENDING // En attente
  PAID // Payée
  OVERDUE // En retard
  CANCELLED // Annulée
}

// 🔹 Modèle des Factures
model Invoice {
  id            String @id @default(uuid())
  invoiceNumber String @unique 
  
  // 🔹 Informations générales sur la facture
  name          String
  issuerName    String?
  issuerAddress String?
  clientName    String?
  clientAddress String?

  // 🔹 Dates clés de la facture
  invoiceDate DateTime  @default(now())
  dueDate     DateTime?

  // 🔹 TVA et statut de la facture
  vatActive Boolean       @default(false)
  vatRate   Float         @default(20)
  status    InvoiceStatus @default(PENDING)

  // 🔹 Montant total de la facture
  totalAmount Float @default(0)

  // 🔹 Relation avec les lignes de facturation
  lines InvoiceLine[] @relation("InvoiceLines")

  // 🔹 Relation avec l'utilisateur qui a créé la facture
  userId      String
  createdById User   @relation("UserInvoices", fields: [userId], references: [id])

  // 🔹 Timestamps pour le suivi
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model InvoiceLine {
  id String @id @default(uuid())

  // 🔹 Description de l'article ou du service facturé
  description String?

  // 🔹 Détails de la quantité et des prix
  quantity   Int   @default(1) // Quantité par défaut : 1
  unitPrice  Float @default(0) // Prix unitaire
  totalPrice Float @default(0) // Prix total (calculé)

  // 🔹 Relation avec la facture associée
  invoiceId String?
  invoice   Invoice? @relation("InvoiceLines", fields: [invoiceId], references: [id], onDelete: Cascade)

  // 🔹 Timestamps pour le suivi des modifications
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
